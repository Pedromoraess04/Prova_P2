const Contract = require('./models/Contract');
const Deposit = require('./Deposit');
const Job = require('./Job');
const Payment = require('./Payment');
const Profile = require('./profile');

module.exports = { Contract, Deposit, Job, Payment, Profile };
